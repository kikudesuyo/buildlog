# AI Agent Rules

## Go Naming Conventions
- Go言語における配列またはスライス（slice/array）変数の命名は、複数形（例: `diaries`, `techs`, `apps` 等）ではなく、語尾に `List`（例: `diaryList`, `techList`, `appList`, `postList`）を付与する命名形式に統一してください。

# AIエージェントへの指示

## UI確認とスクリーンショット

- UI変更では、モバイル幅とPC幅の両方で主要ページを確認する。
- PR descriptionにスクリーンショットを添付する場合、元画像の解像度ではなく、description内の画像タグで表示幅を指定する。
- モバイルのスクリーンショットは`<img ... width="300">`、PCのスクリーンショットは`<img ... width="400">`で表示する。
- スクリーンショットの元画像をリサイズして幅を合わせるのではなく、画像タグの`width`属性で表示サイズを指定する。

## GitHub文書

- Issue、Pull Request、description、コメントは日本語で記載する。
- Markdownの改行を保持するため、本文は`.md`ファイルに書き、GitHub CLIの`--body-file`で登録する。

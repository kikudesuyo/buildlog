# AIエージェントへの指示

## UI確認とスクリーンショット

- UIを変更した場合は、PRを作成する前に必ずブラウザで確認し、PR descriptionへ証跡を添付する。添付は任意ではない。
- 確認対象は、変更したすべての主要ページ・状態とする。少なくともモバイル幅とPC幅を各1枚撮影する。モバイル幅は375px前後、PC幅は1280px前後を使用する。
- クリック、入力、モーダル、ページ遷移、追加読み込みなどの操作を変更した場合は、操作前後が分かる動画も撮影する。動画で示す必要がない単純な見た目の変更はスクリーンショットでよい。
- ブラウザ操作には、利用可能なブラウザ操作ツールを使用する。静的解析やコード確認だけでUI確認を完了扱いにしてはいけない。
- PR descriptionに添付する画像は、元画像をリサイズせず、Markdown内のHTML画像タグで表示幅を指定する。
- モバイルの画像タグは`<img src="..." alt="モバイル表示" width="300">`、PCの画像タグは`<img src="..." alt="PC表示" width="400">`とする。
- スクリーンショット・動画は撮影後にGitHubへアップロードし、アップロード先URLをPR descriptionへ記載する。ローカルの一時ファイルはアップロード確認後に削除する。
- PR descriptionは`.md`ファイルで作成し、`gh pr create --body-file`または`gh pr edit --body-file`で登録する。本文へ画像タグと動画リンクが実際に含まれていることを登録後に確認する。
- ブラウザ起動、認証、アップロード、表示確認のいずれかができない場合は、スクリーンショットなしでPRを完了扱いにせず、未実施の理由を報告して確認を求める。
- UI変更の作業完了前チェックリスト:
  1. モバイル幅で主要ページを確認・撮影した
  2. PC幅で主要ページを確認・撮影した
  3. 操作変更がある場合は動画を撮影した
  4. 画像・動画をGitHubへアップロードした
  5. PR descriptionに画像タグ・動画リンクを追加した
  6. PR上で画像・動画が表示できることを確認した
  7. ローカルの一時ファイルを削除した

## GitHub文書

- Issue、Pull Request、description、コメントは日本語で記載する。
- Markdownの改行を保持するため、本文は`.md`ファイルに書き、GitHub CLIの`--body-file`で登録する。

## PR作成後の検証

- PR作成後の確認で本番データベースへ migration を適用してはいけません。
- migration を変更した場合は、次の検証コマンドを実行してください。
  - `cd sql && atlas migrate validate --dir "file://migrations"`
  - `cd api && GOCACHE=/tmp/buildlog-go-cache go test ./...`
  - `git diff --check`
- 実行したコマンドと結果（成功・失敗、既知の警告を含む）を PR description に記載してください。
- 本番 migration は、承認済みのデプロイ・運用手順またはGitHub Actionsから実行してください。

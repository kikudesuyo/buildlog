# AIエージェントへの指示

## 最重要: 作業完了の定義

Issue・タスクを実装しただけでは作業完了ではありません。

特別な指示がない限り、以下をすべて完了して初めて作業完了としてください。

1. 最新の `main` を取得する
2. 作業用branchを作成する
3. 実装する
4. 必要なテスト・静的解析を実行する
5. UI変更がある場合はブラウザで動作確認する
6. UI変更がある場合はスクリーンショット・必要に応じて動画を撮影する
7. commitする
8. remoteへpushする
9. Pull Requestを作成する
10. PR descriptionへ検証結果を記載する
11. UI変更がある場合はPR descriptionへスクリーンショット・動画を添付する
12. 作成したPRを確認し、必要な情報が実際に反映されていることを確認する

**コード変更、commit、pushだけで作業を終了してはいけません。必ずPR作成まで行ってください。**

PRを作成できない場合のみ、その理由と、どの手順まで完了したかを明示して作業を終了してください。

---

## 作業開始時のGit操作

複数Agent・複数ユーザーが同時に開発するため、古いbranchを起点に作業してはいけません。

作業開始時は必ず最新の `main` を取得してください。

例:

```bash
git switch main
git pull --ff-only
git switch -c <branch-name>
```

既存の作業branchを使用する必要がある場合も、作業開始前にremoteの最新状態を確認してください。

---

## UI変更時の必須確認

UIを変更した場合、**スクリーンショットまたは動画のないPRを完成扱いにしてはいけません。**

コードや静的解析だけを確認してUI確認済みとしてはいけません。必ず実際のブラウザを使用してください。

### ブラウザ確認

ローカル環境は以下で起動します。

API:

```bash
cd api
make dev
```

Web:

```bash
cd web
pnpm run dev
```

利用可能なブラウザ操作ツールを使用して、変更した画面を実際に開いて確認してください。

### 必須スクリーンショット

UI変更がある場合、少なくとも以下を撮影してください。

- モバイル: 375px前後
- PC: 1280px前後

変更したすべての主要ページ・主要状態を確認対象とします。

単一ページの変更であっても、原則としてモバイル・PCの両方を撮影してください。

### スクリーンショットの検証サイズ

DevTools のデバイスツールバーなどでviewportを設定して確認し、PR description の UI確認欄に検証サイズを明記してください。

- Mobile: `375px × 812px`
- PC: `1280px × 800px`

上記と異なるサイズで確認した場合は、実際に設定したviewportの幅・高さを記載してください。単に「モバイル」「PC」とだけ書いてはいけません。

### 動画が必要な変更

以下のような操作を変更した場合は、スクリーンショットだけではなく、操作前後が確認できる動画も撮影してください。

- クリック
- 入力
- モーダル
- ドロワー
- ページ遷移
- アニメーション
- 追加読み込み
- ドラッグ&ドロップ
- その他インタラクション

単純な見た目だけの変更であれば動画は不要です。

---

## スクリーンショット・動画のPR添付

撮影したスクリーンショット・動画は、PRレビュー担当者がGitHub上から参照できる状態にしてください。

ローカルファイルのパスだけをPR descriptionへ記載してはいけません。

スクリーンショットはリポジトリへコミットしてPR descriptionから参照するのではなく、GitHubから直接参照できるアップロードURLを使用してください。PR description に画像ファイルの相対パス（例: `docs/screenshots/...png`）を記載してはいけません。

GitHubから参照可能なURLへアップロードし、そのURLをPR descriptionへ記載してください。

画像はリサイズしたファイルを生成せず、元画像をそのまま使用し、HTMLの `width` 属性で表示サイズを指定してください。

モバイル:

```html
<img src="IMAGE_URL" alt="モバイル表示" width="300" />
```

PC:

```html
<img src="IMAGE_URL" alt="PC表示" width="400" />
```

動画はGitHub上から参照できるURLを記載してください。

PR作成後、**GitHub上で画像・動画が実際に表示・参照できることを確認してください。**

確認が終わるまでローカルの一時ファイルを削除してはいけません。

---

## PR作成

実装・検証が終了したら、特別な指示がない限り必ずPull Requestを作成してください。

PRはDraftではなく、レビュー可能な状態で作成してください。

```bash
gh pr create --body-file /tmp/pr-body.md
```

`--draft` は指定しないでください。

PR descriptionは直接コマンドライン引数へ埋め込まず、一度Markdownファイルを作成して `--body-file` を使用してください。

PR作成後は `gh pr view` 等を使用して、以下を確認してください。

- PRが作成されている
- PR titleが正しい
- PR descriptionが登録されている
- Markdownの改行が壊れていない
- 検証結果が記載されている
- UI変更の場合、画像・動画が記載されている

**PR URLを取得するまでPR作成完了としてはいけません。**

---

## PR description

PR descriptionには最低限以下を記載してください。

```md
## 概要

変更内容を簡潔に記載する。

## 変更内容

- xxx
- xxx

## 動作確認

- `command`: 成功
- `command`: 成功

## UI確認

<!-- UI変更がある場合 -->

### Mobile

<img src="..." alt="モバイル表示" width="300">

### PC

<img src="..." alt="PC表示" width="400">

### 動画

<!-- 必要な場合 -->

動画URL

## 補足

必要であれば記載する。
```

UI変更がない場合は、`UI変更なし` と記載してください。

---

## 自己レビュー

実装・検証の完了前に、変更内容を実装者自身がレビューしてください。テストが成功しただけでは自己レビュー完了とはみなしません。少なくとも以下を確認し、問題があれば修正してからPRを作成します。

- **コンフリクト**: 最新の `main` と比較し、マージベースが古くないことを確認する。コンフリクトマーカー（`<<<<<<<`、`=======`、`>>>>>>>`）が残っていないこと、意図しないマージコミットや変更の巻き戻しがないことを確認する。
- **既存のコード規則**: 変更対象と周辺の既存コードを読み、ディレクトリ構成、レイヤー責務、命名、エラーハンドリング、共通コンポーネント、1機能1ファイルの原則に沿っていることを確認する。既存の実装と異なる方式を追加する場合は、その理由をPR descriptionに記載する。
- **セキュリティ**: 認証・認可の抜け、入力値検証不足、SQLインジェクション、XSS、CSRF、SSRF、機密情報のログ出力・ハードコード、危険なファイル操作、依存関係の不要な追加がないかを確認する。ユーザー入力・外部データ・権限境界を変更した場合は、正常系だけでなく拒否されるべきケースも確認する。
- **影響範囲**: 変更ファイルだけでなく、そのAPI利用箇所、DB・migration、UI状態、既存テストへの影響を確認する。不要な変更やデバッグコードが含まれていないことを確認する。
- **差分と検証**: `git diff main...HEAD`、`git diff --check`、format・lint・testの結果を確認し、PR descriptionに実行結果を記載する。失敗や既知のwarningを隠さず記載する。

自己レビューで問題が見つかった場合は、指摘事項と修正内容を確認し、再度自己レビューを実施してください。未解決の指摘、コンフリクト、セキュリティ上の懸念が残っている場合は、作業完了やPR作成完了として報告してはいけません。

---

## AI Agentの実装・レビューサイクル

AI Agentは、実装を一度行って終了せず、以下のサイクルを自律的に繰り返してください。

1. **実装**: Issueと既存コードを確認し、必要最小限の変更を行う。
2. **検証**: format、lint、test、`git diff --check`を実行する。UI変更がある場合はブラウザ確認も行う。
3. **レビュー**: 自己レビュー項目に沿って、コンフリクト、既存コード規則、セキュリティ、影響範囲、テスト不足を確認する。
4. **判定**:
   - 指摘や検証失敗がある場合は、指摘事項を整理して実装へ戻る。
   - 指摘がなく、検証がすべて成功した場合は、そのサイクルを `APPROVED` として終了する。
5. **再確認**: 修正後は変更された箇所だけでなく、修正による周辺機能への影響を確認してから、再度レビューする。

各サイクルでは、`実装 → 検証 → レビュー → 判定` の結果を作業メモまたはPR descriptionに記録してください。レビューを通過していない状態でcommit、push、PR作成完了、作業完了として報告してはいけません。

同じ指摘や検証失敗が3サイクル連続で解消できない場合は、無理にループを継続せず、原因・試した対応・必要なユーザー判断を報告して停止してください。仕様の曖昧さ、権限不足、外部サービス障害など、Agentだけで解決できない場合も同様です。

---

## GitHub文書

以下は日本語で記載してください。

- Issue
- Pull Request title
- Pull Request description
- GitHubコメント

Markdown本文は `.md` ファイルを作成し、GitHub CLIの `--body-file` を使用してください。

---

## migration変更時の検証

PR作成前の確認で、本番データベースへmigrationを適用してはいけません。

migrationを変更した場合は、最低限以下を実行してください。

```bash
cd sql && atlas migrate validate --dir "file://migrations"
```

```bash
cd api && GOCACHE=/tmp/buildlog-go-cache go test ./...
```

```bash
git diff --check
```

実行したコマンドと結果をPR descriptionへ記載してください。

成功だけではなく、失敗や既知のwarningがある場合も記載してください。

本番migrationは、承認済みのデプロイ・運用手順またはGitHub Actionsからのみ実行してください。

---

## UI変更時の完了チェック

UI変更がある場合、作業終了前に以下をすべて確認してください。

- [ ] モバイル幅で主要ページ・状態を確認した
- [ ] モバイル幅のスクリーンショットを撮影した
- [ ] PC幅で主要ページ・状態を確認した
- [ ] PC幅のスクリーンショットを撮影した
- [ ] 操作変更がある場合は動画を撮影した
- [ ] スクリーンショット・動画をGitHubから参照可能にした
- [ ] PR descriptionへ画像・動画を追加した
- [ ] PRを作成した
- [ ] GitHub上でPR descriptionを確認した
- [ ] GitHub上で画像・動画が参照できることを確認した
- [ ] PR URLを取得した
- [ ] 必要な検証結果をPR descriptionへ記載した
- [ ] 確認完了後に一時ファイルを削除した

**1つでも未完了の場合、UI変更タスクを「完了」と報告してはいけません。**

---

## 作業を停止してよい条件

次の場合を除いて、PR作成まで自律的に進めてください。

- 必要な認証情報がなく、作業を継続できない
- GitHubへのpush権限がない
- GitHubへのPR作成権限がない
- ブラウザを起動できない
- 必要なローカルサービスを起動できない
- スクリーンショット・動画をGitHubから参照可能な状態にできない
- ユーザー判断がなければ安全に進められない重大な仕様の曖昧さがある

上記の場合は、可能なところまで作業を進めた上で、

- 何が完了しているか
- 何が未完了か
- なぜ実行できなかったか
- ユーザー側で必要な対応

を報告してください。

単に「PRを作った方がよい」「スクリーンショットを撮った方がよい」と提案して終了してはいけません。

---

## 最終報告

作業終了時には最低限以下を報告してください。

- 実装内容
- 実行したテスト・検証
- UI確認内容
- commit
- PR URL

UI変更がある場合は、スクリーンショット・動画がPRへ添付済みであることも明記してください。
